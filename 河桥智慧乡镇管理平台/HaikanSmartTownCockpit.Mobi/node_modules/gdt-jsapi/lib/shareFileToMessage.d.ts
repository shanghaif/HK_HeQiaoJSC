import { SuccessResult } from './invoker';
export interface ShareFileToMessageParams {
    fileName: string;
    fileUrl: string;
}
declare function shareFileToMessage(args: ShareFileToMessageParams): Promise<SuccessResult>;
declare namespace shareFileToMessage {
    var version: {
        android: string;
        ios: string;
        pc: string;
    };
}
export default shareFileToMessage;
