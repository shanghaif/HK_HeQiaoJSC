export interface TitleParams {
    title: string;
}
declare function setTitle(args: TitleParams): Promise<{}>;
declare namespace setTitle {
    var version: {
        android: string;
        ios: string;
        pc: string;
    };
}
export default setTitle;
